name: Pull Request Test

on:
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened ]
    paths-ignore: # なぜか上手く機能しない
      - 'docs/**'


jobs:
  test:
    name: Test
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      pull-requests: write
    timeout-minutes: 1
  
    steps:
      - run: exit 0 # intentionally fail
      - uses: actions/checkout@v3